log_level:
  - INFO
  - WARNING
  - ERROR
  - DEBUG
  - DEBUG_LVL2
local: true #if true, we won't use provided webhookURL but http://localhost: or https://localhost: depending if Security is enabled
authorization: true  #select true if using Security in Elasticsearch
checkCerts: true  #select true if plugin should check certs provided by Elastic
elasticsearch_port: 9200
webhookURL: yourWebhookUrl
apiKey: yourApiKey
index_pattern: yourIndexPattern
truststore_path: pathToCertFromYourElasticServer
truststore_password: yourPassword

anti-cheat:
  enabled: true
  violation-history-seconds: 120
  checks:
    reach:
      warning-distance: 4.0
      critical-distance: 5.0
      warning-severity: 6
      critical-severity: 7
    kill-aura:
      line-of-sight-severity: 6
      same-tick-hit-severity: 6.5
      max-hits-per-tick: 1
      switch-interval-ms: 50
      switch-violation-severity: 6.5
  thresholds:
    - severityMin: 5
      countMin: 2
      windowSeconds: 45
      command: "warn %player_name% [Reach] severity=%severity% count=%count%"
    - severityMin: 7
      countMin: 3
      windowSeconds: 90
      command: "kick %player_name% Excessive violations (%check_type%)"


